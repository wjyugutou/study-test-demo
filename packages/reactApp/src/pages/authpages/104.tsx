import type { FC } from 'react'
import { useCallback, useEffect, useMemo, useRef, useState } from 'react'

interface Props {
}

const App: FC<Props> = () => {
  return <div>auth 104</div>
}

export default App
